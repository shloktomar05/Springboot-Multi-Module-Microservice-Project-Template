server:
  port: 9013

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/Multi_Module_Project
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
